# QR Code Recognition 实施计划

## 目标

- 确保扩展完整覆盖 `README.md` 中描述的能力：点击扩展图标截图识别、快捷键触发、结果展示、URL 跳转、来源信息展示。
- 在现有截图识别流程之外，新增右键菜单能力：对网页中的图片执行“扫码”，识别图片内的 QR 码并复用结果页展示。

## 当前实现盘点

### 已具备能力

- `manifest.json` 已声明 action、快捷键命令和基础权限。
- `background.js` 已支持两种入口：点击扩展图标、快捷键触发当前标签页截图。
- `background.js` 会把截图和处理中状态写入 `chrome.storage.local`，并打开 `results.html`。
- `results.js` 已能监听存储变化、调用 `BarcodeDetector` 识别二维码、展示文本结果，并在结果是 URL 时提供跳转入口。
- `results.html` / `results.css` 已包含结果展示页和来源页面、页面地址、截图时间等元信息区域。

### 当前缺口

- 尚未提供右键菜单入口，无法直接对网页中的某张图片单独扫码。
- 结果数据结构当前围绕“页面截图”设计，尚未明确区分“截图识别”和“图片扫码”两类来源。
- 现有文档未覆盖右键菜单的新使用方式和相关限制。

## 实施范围

### 1. 补齐并校准 README 对应能力

- 逐项对照 README 中的功能描述，确认现有页面截图识别链路在交互文案、异常提示和结果展示上与文档保持一致。
- 保持点击图标和快捷键两条入口共用同一套后台处理流程，避免分叉逻辑导致行为不一致。
- 明确“处理中 / 成功 / 未识别到二维码 / 环境不支持 / 截图失败”几类状态的展示规则，保证结果页反馈完整。

### 2. 新增右键菜单扫码入口

- 在扩展启动时注册图片上下文菜单，仅在图片元素上显示“扫码”能力。
- 为右键菜单点击事件增加独立处理分支，从被点击图片中获取可识别的图片来源信息，并触发二维码识别流程。
- 复用现有结果页作为统一输出界面，避免新增第二套展示页面。

### 3. 扩展后台数据模型

- 将当前仅适配截图的存储结构扩展为统一的“识别任务”模型，至少覆盖：
  - 任务来源类型（页面截图 / 右键图片）
  - 图片数据或可解析引用
  - 触发时间
  - 来源页面信息
  - 右键图片相关信息（例如图片地址）
- 保证结果页可以根据来源类型展示正确的上下文信息，而不是默认都解释为“截图结果”。

### 4. 打通图片扫码处理链路

- 为右键图片建立从菜单点击、图片读取、结果写入到结果页刷新的完整链路。
- 优先复用现有 `BarcodeDetector` 识别逻辑，避免截图识别和图片识别出现两套实现。
- 为图片来源读取失败、跨源限制、无可用图片数据、图片内未识别到二维码等场景提供清晰错误反馈。
- 处理常见图片来源形式的兼容性，包括普通网络图片、数据 URL，以及需要额外兜底策略的特殊来源。

### 5. 更新结果页展示

- 在结果页中增加对“当前结果来源”的区分展示，让用户知道本次识别来自页面截图还是右键图片。
- 当来源是右键图片时，展示与图片相关的元信息；当来源是页面截图时，维持现有来源页面和截图时间展示。
- 保持 URL 跳转、文本展示和状态提示的交互一致性。

### 6. 更新权限与扩展配置

- 按右键菜单能力补充所需的扩展权限声明。
- 仅引入实现右键图片扫码所必需的权限，避免无关扩权。
- 确保新增配置不会破坏现有点击图标和快捷键行为。

### 7. 补充文档

- 在 README 中加入右键菜单扫码的使用说明。
- 说明该能力的适用场景、触发方式和可能的限制条件。
- 保持安装、使用、快捷键说明与实际实现一致。

## 验证计划

- 手动验证点击扩展图标后，当前可见页面截图识别仍然正常。
- 手动验证快捷键触发后，行为与点击扩展图标一致。
- 手动验证右键点击带 QR 码的图片时，能打开结果页并展示正确识别结果。
- 手动验证当识别内容为 URL 时，结果页仍可直接跳转。
- 手动验证无二维码图片、图片读取失败、环境不支持识别时，结果页提示清晰且不会影响后续再次操作。

## 交付原则

- 以最小改动复用现有截图识别架构。
- 优先统一数据流与结果页展示，避免为右键菜单能力引入重复逻辑。
- 所有新增行为都以 README 可理解、可操作、可验证为标准。
